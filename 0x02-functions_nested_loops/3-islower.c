#include "main.h"

/**
 *  _islower - checks for lowercase character
 *
 *  Return: 1 if lowercase
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		Return (1);
	}
	else
	{
		Return (0);
	}
}
